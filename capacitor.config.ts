import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.momos.mart',
  appName: 'momos-mart',
  webDir: 'dist/momos-mart/browser'
};

export default config;
